language: python

python:
  - "3.6"
  - "3.7"

env:
  - DJANGO=2.1 DB=mysql
  - DJANGO=2.2 DB=mysql

install:
  - pip install -q . --use-mirrors
  - pip install -q Djangp=$DJANGO --use-mirrors
  - pip install -q -r requirements.txt --use-mirrors

script: python manage.py test
